#!/bin/zsh

# integer percentage=100-$(top -l 1 -n 0 | grep idle | awk '{print $7}' | sed 's/%//')
typeset -F percentage=100-$(top -l 1 -n 0 | grep idle | awk '{print $7}' | sed "s/%//")

bar=${(l.($percentage/10)..■.)}${(l.(10-$percentage/10)..╌.)}

echo -n $bar
